package main

import (
	"encoding/json"
	"flag"
	"fmt"
	"os"
	"path"
	"regexp"
	"runtime"
	"sort"
	"strconv"
	"strings"

	"github.com/artalkjs/artalk/v2/internal/config"
	"github.com/artalkjs/artalk/v2/internal/config/meta"
	"github.com/artalkjs/artalk/v2/internal/pkged"
	"github.com/samber/lo"
)

var (
	workDir    string
	locale     string
	format     string
	outputFile string
	pkgName    string
)

func init() {
	flag.StringVar(&workDir, "w", "", "specify work directory")
	flag.StringVar(&outputFile, "o", "", "specify save file")
	flag.StringVar(&format, "format", "markdown", "specify output format")
	flag.StringVar(&locale, "locale", "zh-cn", "specify locale")
	flag.StringVar(&pkgName, "pkg", "config", "specify output package name")
}

func main() {
	flag.Parse()

	// change WorkDir run on project root
	if workDir != "" {
		if err := os.Chdir(workDir); err != nil {
			panic(err)
		}
	}

	// load assets fs
	{
		_, filename, _, _ := runtime.Caller(0)
		rootDir := path.Join(path.Dir(filename), "../../../../")
		dirFS := os.DirFS(rootDir)
		pkged.SetFS(dirFS)
	}

	// get metadata
	confTemplate := config.Template(locale)
	metas, err := meta.GetOptionsMetaData(confTemplate)
	if err != nil {
		panic(err)
	}

	// get mapping data
	mapping := meta.GetEnvPathMap(config.Config{}, confTemplate)

	// output
	result := ""
	switch format {
	case "go":
		result = getGo(metas, mapping)
	case "json":
		result = getJSON(metas)
	case "markdown":
		result = getMarkdown(metas, outputFile)
	default:
		panic("invalid output format")
	}

	// write
	if outputFile == "" {
		fmt.Println(result)
	} else {
		switch format {
		case "markdown":
			writeMarkdown(outputFile, result)
		case "go", "json":
			file, err := os.Create(outputFile)
			if err != nil {
				fmt.Println("Error creating file:", err)
				return
			}
			defer file.Close()

			_, err = file.WriteString(result)
			if err != nil {
				fmt.Println("Error writing to file:", err)
			}

			fmt.Println("File written successfully")
		default:
			panic("the format does not support writing to a file")
		}
	}
}

func formatMap(m map[string]string) string {
	// To ensure consistent output, we'll sort the map keys.
	keys := make([]string, 0, len(m))
	for k := range m {
		keys = append(keys, k)
	}
	sort.Strings(keys)

	str := fmt.Sprintf("%T{\n", m)
	for _, k := range keys {
		str += fmt.Sprintf("\t%#v: %#v,\n", k, m[k])
	}
	str += "}"
	return str
}

func formatOptionsMetaSlice(s []meta.OptionsMeta) string {
	// To ensure consistent output, we'll sort by meta.OptionsMeta.Env
	sorted := make([]meta.OptionsMeta, len(s))
	copy(sorted, s)
	sort.Slice(sorted, func(i, j int) bool {
		return sorted[i].Env < sorted[j].Env
	})

	str := fmt.Sprintf("%T{\n", s)
	for _, item := range sorted {
		str += fmt.Sprintf("\t%#v,\n", item)
	}
	str += "}"
	return str
}

func getGo(metas []meta.OptionsMeta, mapping map[string]string) string {
	str := "// Code generated by go run internal/config/meta/gen/main.go. DO NOT EDIT.\n\n"
	str += "package " + pkgName + "\n\nimport \"github.com/artalkjs/artalk/v2/internal/config/meta\"\n\n"
	str += "// Cache result of `meta.GetEnvPathMap(config.Config{}, config.Template(\"" + locale + "\"))`\n"
	str += "var EnvPathMapCache = " + formatMap(mapping) + "\n\n"
	str += "// Cache result of `meta.GetOptionsMetaData(config.Template(\"" + locale + "\"))`\n"
	str += "var OptionsMetaCache = " + formatOptionsMetaSlice(metas) + "\n"
	return str
}

func getJSON(metas []meta.OptionsMeta) string {
	str, err := json.MarshalIndent(metas, "", "  ")
	if err != nil {
		panic(err)
	}
	return string(str)
}

func getMarkdown(metas []meta.OptionsMeta, templateFile string) string {
	generalTableTitle, tableFields := "通用配置", []string{"环境变量", "默认值", "描述", "路径"}

	if templateFile != "" {
		if _, err := os.Stat(templateFile); err == nil {
			_, _, _, generalTableTitle, tableFields = parseMarkdownEnv(templateFile)
		}
	}
	md := ""

	// Get root nodes
	rootNodes := []meta.OptionsMeta{
		{
			Title:  generalTableTitle,
			Desc:   generalTableTitle,
			Path:   "general",
			IsRoot: true,
		},
	}
	rootNodes = append(rootNodes, lo.Filter(metas, func(m meta.OptionsMeta, _ int) bool {
		return m.IsRoot && m.HasChild
	})...)

	printGroup := func(root meta.OptionsMeta, items []meta.OptionsMeta) {
		md += fmt.Sprintf("## %s\n\n", root.Title)
		md += "| " + strings.Join(tableFields, " | ") + " |\n"
		md += fmt.Sprintln("| --- | --- | --- | --- |")
		for _, m := range items {
			if m.Title == "Enabled" {
				m.Title = "启用"
			}
			options := lo.If(len(m.Options) > 0, "(可选：`["+strings.Join(lo.Map(m.Options, func(o string, _ int) string { return strconv.Quote(o) }), ", ")+"]`) ").Else("")
			desc := lo.If(m.Desc != "", "("+m.Desc+") ").Else("")

			defaultValue := m.Default
			if t, ok := m.Default.(string); ok {
				defaultValue = strconv.Quote(t)
			}

			md += fmt.Sprintf("| **%s** | `%v` | %s %s%s| %s (%s) |\n", m.Env, defaultValue, m.Title, desc, options, m.Path, m.PathText)
		}
		md += "\n\n"
	}

	for _, root := range rootNodes {
		printGroup(root, lo.Filter(metas, func(m meta.OptionsMeta, _ int) bool {
			if root.Path == "general" {
				return m.AllowsSet && m.IsRoot
			} else {
				return m.AllowsSet && !m.IsRoot && strings.HasPrefix(m.Path, root.Path)
			}
		}))
	}

	return strings.TrimSpace(md)
}

var start = "<!-- env-variables -->"
var end = "<!-- /env-variables -->"
// parseMarkdownEnv extracts the env.md to be:
// - header: content before the env variables section
// - body: the content between start and end markers
//   - firstTableTitle: the first title (## ...) after the start marker
//   - tableFields: the first table header row after the first table title
// - footer: content after the env variables section
//
// For example, given a snippet from env.md:
// ```md
// ...header...
// <!-- env-variables -->
// ## 通用配置
//
// | 环境变量 | 默认值 | 描述 | 路径 |
// | --- | --- | --- | --- |
// | **ATK_ADMIN_USERS** | `[]` | 管理员账户 | admin_users (管理员账户) |
// ...more tables...
// <!-- /env-variables -->
// ...footer...
// ```
// The function should return:
//   header: "...header...\n"
//   body: "\n## 通用配置\n\n| 环境变量 | 默认值 | 描述 | 路径 |\n| --- | --- | --- | --- |\n| **ATK_ADMIN_USERS** | `[]` | 管理员账户 | admin_users (管理员账户) |\n...more tables...\n"
//   footer: "\n...footer..."
//   firstTableTitle: "通用配置"
//   tableFields: ["环境变量", "默认值", "描述", "路径"]
func parseMarkdownEnv(file string) (header, body, footer, firstTableTitle string, tableFields []string) {
	contentBytes, err := os.ReadFile(file)
	if err != nil {
		panic(err)
	}
	content := string(contentBytes)

	startIdx := strings.Index(content, start)
	endIdx := strings.Index(content, end)
	if startIdx == -1 || endIdx == -1 || endIdx < startIdx {
		panic(fmt.Sprintf("%s markers not found or invalid", start))
	}

	header = content[:startIdx]
	body = content[startIdx+len(start) : endIdx]
	footer = content[endIdx+len(end):]

	// Find the first title (## ...) after the start marker
	afterStart := content[startIdx+len(start):]
	titleIdx := strings.Index(afterStart, "## ")
	if titleIdx == -1 {
		firstTableTitle = ""
	} else {
		// Find the end of the line
		titleLineEnd := strings.Index(afterStart[titleIdx:], "\n")
		if titleLineEnd == -1 {
			firstTableTitle = strings.TrimSpace(afterStart[titleIdx+3:])
		} else {
			firstTableTitle = strings.TrimSpace(afterStart[titleIdx+3 : titleIdx+titleLineEnd])
		}
	}

	// Find the first table header after the general table title
	tableHeaderIdx := -1
	if titleIdx != -1 {
		afterTitle := afterStart[titleIdx:]
		tableHeaderIdx = strings.Index(afterTitle, "|")
		if tableHeaderIdx != -1 {
			// The first line starting with '|' is the table header
			tableHeaderLineEnd := strings.Index(afterTitle[tableHeaderIdx:], "\n")
			var tableHeaderLine string
			if tableHeaderLineEnd == -1 {
				tableHeaderLine = afterTitle[tableHeaderIdx:]
			} else {
				tableHeaderLine = afterTitle[tableHeaderIdx : tableHeaderIdx+tableHeaderLineEnd]
			}
			// Split and trim fields
			fields := strings.Split(tableHeaderLine, "|")
			for _, f := range fields {
				f = strings.TrimSpace(f)
				if f != "" {
					tableFields = append(tableFields, f)
				}
			}
		}
	}

	// Remove empty fields at the start/end (from leading/trailing '|')
	if len(tableFields) > 0 && tableFields[0] == "" {
		tableFields = tableFields[1:]
	}
	if len(tableFields) > 0 && tableFields[len(tableFields)-1] == "" {
		tableFields = tableFields[:len(tableFields)-1]
	}

	body = strings.Trim(body, "\n")
	return
}

func writeMarkdown(file string, text string) {
	// read file content
	content, err := os.ReadFile(file)
	if err != nil {
		panic(err)
	}

	re := regexp.MustCompile(start + `(?s:(.*?))` + end)
	matches := re.FindAllStringSubmatch(string(content), -1)
	if len(matches) == 0 {
		panic(fmt.Sprintf("%s not found", start))
	}

	// replace
	newContent := strings.ReplaceAll(string(content), matches[0][0], start+"\n\n"+text+"\n\n"+end)

	// write file
	if err := os.WriteFile(file, []byte(newContent), 0644); err != nil {
		panic(err)
	}

	fmt.Println("File updated successfully")
}
